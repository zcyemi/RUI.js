import { RUIRectangle } from "../RUIRectangle";
import { RUICmdList } from "../RUICmdList";


export class RUITestWidget extends RUIRectangle{

    public constructor(){
        super();
    }
    public onDraw(cmd:RUICmdList){
        super.onDraw(cmd);
    }

}